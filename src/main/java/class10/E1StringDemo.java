package class10;

public class E1StringDemo {
    public static void main(String[] args) {

     //we have created an object from the string clas and have stored "Cat" inside it
        String obj=new String("Cat");

        //Shortcut to create obj from String class
        String obj2="My Dog"; //String obj2=new String("Dog") <the shortcut only works for String> this method is used more

        int len=obj2.length(); //calling the length method/function/action/behaviour
        System.out.println(len);




    }
}
